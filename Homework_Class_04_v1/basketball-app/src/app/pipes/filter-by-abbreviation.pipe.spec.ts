import { FilterByAbbreviationPipe } from './filter-by-abbreviation.pipe';

describe('FilterByAbbreviationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAbbreviationPipe();
    expect(pipe).toBeTruthy();
  });
});
